#include <stdio.h>

/**
 * main - Entry point
 * autor: Ayomide0123
 * Return: Always 0 (Success)
 */
int main(void)
{
	int ayo;
	char mide;

	for (ayo = 48; ayo < 58; ayo++)
		putchar(ayo);
	for (mide = 'a'; mide <= 'f'; mide++)
		putchar(mide);
	putchar('\n');
	return (0);
}
